# Supabase — fill these in to switch from the local seed file to the shared backend.
# Leave empty and the app runs fully on src/data/seed.json (offline-friendly).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
